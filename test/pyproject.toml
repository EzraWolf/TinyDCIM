[project]
name = "TinyDCIM-Test"
version = "0.1.0"
description = "Test package using CoboTB for TinyDCIM"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "cocotb>=2.0.1",
]

[dependency-groups]
dev = [
    "ruff>=0.15.1",
]
